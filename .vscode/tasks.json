{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "CMake: make",
			"command": "make clean -s && make -s", // && flips --apply hack.ips poketcg2.gbc poketcg2-patched.gbc
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": [],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}